---
name: native-output
description: 한국어 산출물 전반에 적용하는 글쓰기 기준. 번역투, AI 상투 표현, 영어식 구성을 걸러내고 글 종류에 맞는 구조와 리듬으로 쓴다. 문서, 보고서, 커밋 메시지, 코드 주석, 화면 문구, 발표 자료, 대화 응답 등 한국어가 포함된 모든 작업에 사용한다. "자연스럽게", "AI 티 빼줘", "다듬어줘" 에 반응한다.
---

# native-output

## 개요

- 정의: 한국어 산출물에 잘 쓴 글의 기준을 적용하는 스킬이다.
- 적용 범위: 한국어가 포함된 모든 산출물. 사용자 호출 여부와 무관하게 적용한다.
- 예외: 남의 글을 원문 그대로 인용하는 경우와, `NATIVE_OUTPUT_GATE=off` 로 검사를 끄고 기계 생성물을 다루는 경우.
- 구성: 기준은 `standards/` 의 레이어 문서가 갖고, 이 문서는 적용 절차를 정한다. `install/` 의 훅과 전역 규칙이 강제를 맡는다.

## 절차

### 1. 쓰기 전 질문 (`standards/judgment.md`)

왜 쓰는가 → 어디에 실리고 누가 읽는가 → 메시지 → 들어가야 할 내용 → 형식 → 문장.

- 글 종류와 말투는 이 질문들의 결과로 정한다.
- 기존 글 수정: 착수 전에 목적 대비 내용(누락, 군더더기)을 판정하고, 문제가 있으면 먼저 보고한다.
- 확인되지 않은 항목은 추측으로 채우지 않고 사용자에게 묻는다.

### 2. 적용 문서 매핑

공용 문서 세 가지(`judgment.md`, `korean.md`, `style.md`)는 모든 산출물에 항상 적용한다. 아래 표는 산출물 유형에 따라 그 위에 더할 문서를 정한다.

| 산출물 | 추가로 여는 문서 |
|---|---|
| 블로그, 회고 | `discourse.md`, `genres/blog.md` |
| 에세이, 감상문 | `discourse.md`, `genres/essay.md` |
| 보고서, 분석 | `discourse.md`, `genres/report.md` |
| 필기, 정리 | `genres/notes.md` |
| SNS 포스트, 답글 | `genres/short-posts.md` |
| 지시문 | `genres/instructions.md` |
| 발표 자료 | `slides.md`, `genres/presentation.md` |
| 사내 참조 문서, 매뉴얼, FAQ, 안내서 | `reference.md` |
| 개발 문서, 코드 주석, 결정 기록, 명세 | `dev-prose.md` |
| 앱과 웹의 화면 문구 | `ui-copy.md` |
| 이메일, 메시지 초안 | 없음. 말투와 형식은 받는 사람과 관례로 정한다 |
| 대화 응답 | 없음 |

- `discourse.md` 는 처음부터 끝까지 읽는 글에만 적용한다. 항목을 찾아 읽는 참조 문서와 개발 문서에는 적용하지 않는다.
- 표에 없는 산출물은 공용 문서 세 가지만으로 쓴다. 공용 문서는 글 종류와 무관하게 완전하게 동작하며, 다른 종류의 말투와 구성을 기본값처럼 빌리지 않는다.
- 말투가 불확실하면 사용자에게 확인한다.
- 충돌하면 더 구체적인 문서를 우선한다. 검사는 한 번만 수행한다.

### 3. 자가 검사

- 2번 매핑에 있는 문서를 실제로 열어 적용했는가.
- 말투가 일관성을 유지하고 있는가.
- 문장 길이와 쉼표 밀도가 종류별 팩의 수치 범위에 드는가.
- `standards/banned-words.json` 의 표현이 포함되지 않았는가.
- 남은 상투어가 해당 글 종류의 실사용 표현인가 (`genres.md` 판정표).

### 4. 검수

공개용이거나 고위험인 산출물은 `guides/review.md` 의 절차로 독립 검수한다.

## 계약

- 자연스러움 우선: 최종 판정 기준은 금지 목록 일치 여부가 아니라 그 문장이 자연스러운 한국어인가이다. 규칙의 기계적 적용으로 이미 자연스러운 문장을 고치지 않는다.
- 일괄 치환 금지: 교정은 원문을 읽고 문장 단위로 다시 쓴다.
- 손수치 0: 수치는 실측값만 인정한다. 팩에 없는 글 종류는 가장 가까운 글 종류로 대용하고 그 사실을 명시한다.
- 예시 우선: 사용자가 제공한 예시 글과 이미 승인된 원문이 동봉 기준보다 우선한다.
- 대화 응답: 글 종류 레이어를 제외하고 표현·표기 기준과 금지 목록을 적용한다.

## 원문 보존 수정

다른 사람의 글, 사실이 확정된 원고, 사용자가 보존을 요청한 글을 고칠 때만 적용하는 특수 모드다. 일반 작성과 수정에는 적용하지 않는다.

- 의미를 바꾸지 않는다. 사실, 주장, 수치, 인용, 순서를 그대로 유지한다.
- 변경률을 5~30% 범위로 유지한다. 5% 미만이면 덜 고친 것이고, 30%를 넘으면 과도한 수정이므로 되돌릴지 검토한다.
- 모호한 명사를 다른 명사로 바꿀 때는 원문에서 단정한 부분만 근거로 삼는다. 추측하는 문장을 근거로 삼지 않고, 마땅한 명사가 없으면 서술어로 풀어 쓴다.
- 완료 후 `guides/review.md` 의 절차로 독립 검수한다.

일반 작성과 수정에는 변경률 제한이 없다. 목적에 맞는지로 판단한다.

## setup 절차 (최초 1회)

사람용 안내는 `guides/setup.md` 가 갖는다. 에이전트는 아래를 순서대로 실행하고, 실패한 단계에서 멈추고 보고한다.

1. 위치 확인: 스킬 폴더가 `~/.claude/skills/native-output/` 에 있는지 확인한다. 다른 위치면 그 경로로 복사한 뒤 진행한다.
2. 실행 환경 확인: `node --version`. node 가 없으면 설치를 안내하고 훅 등록을 건너뛴다.
3. 강도 선택: 사용자에게 묻고 답을 받는다.
   - `block`: 위반하면 편집을 거부한다. 기본으로 권장한다.
   - `warn`: 알리기만 한다.
   - `off`: 훅을 걸지 않는다.
4. 훅 등록: `node install/scripts/setup-hooks.js --level=<선택>`. 재실행하면 기존 등록을 지우고 새 강도로 다시 넣는다.
5. 전역 규칙 설치: `node install/scripts/setup-rules.js`. `~/.claude/rules/` 에 규칙이 복사되어 세션마다 자동으로 로드된다.
6. 동작 검증: 한국어가 담긴 Write 입력 JSON 을 표준 입력으로 `node install/hooks/native-output-gate.js` 에 넣어 강도에 맞는 응답을 확인하고 결과를 보여 준다.
7. 완료 보고: 훅과 규칙은 다음 세션부터 적용되므로 재시작을 안내한다.

## 전역 배선

- `install/rules/native-output.md`: `~/.claude/rules/` 에 설치되는 전역 규칙이다. 스킬을 부르지 않은 세션에도 로드된다.
- `install/hooks/native-output-gate.js`: 한국어가 든 Write 와 Edit 직전에 `standards/banned-words.json` 을 대조하고, 스킬을 먼저 불렀는지 확인한다.
- `install/hooks/native-output-mark.js`: 스킬이 로드되면 세션 표시를 남긴다.
- `install/hooks/native-output-stop-gate.js`: 턴이 끝날 때 마지막 대화 응답을 같은 목록과 대조한다. 코드블록과 인용은 제외한다.
- 강도 변경: `node install/scripts/setup-hooks.js --level=<강도>`.
- 검사 해제: 남의 글이나 기계 생성물을 다룰 때 `NATIVE_OUTPUT_GATE=off` 를 쓴다.
