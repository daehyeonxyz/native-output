---
name: native-output
description: 한국어로 무엇이든 쓰거나 고칠 때 항상 적용한다. 번역투와 AI 상투 표현과 영어식 구성을 걸러내고 장르에 맞는 구조와 리듬으로 쓴다. 문서, 보고서, 블로그, 커밋 메시지, 코드 주석, 화면 문구, 발표 자료, 대화 응답까지 한국어가 들어가는 산출물 전부가 대상이다. "자연스럽게", "AI 티 빼줘", "다듬어줘", "한국어로 써줘" 에 반응한다.
---

# native-output

한국어 산출물을 잘 쓴 글로 만든다. 목표는 사람이 쓴 것처럼 읽히는 글이 아니라 **잘 쓴 글**이다. AI 티를 지우는 것은 그 결과이지 목적이 아니다.

## 적용 범위

항상 적용한다. 한글이 한 글자라도 들어가는 산출물 전부가 대상이고, 사용자가 부르지 않아도 적용한다. 문서와 보고서, 블로그와 회고, 커밋 메시지와 PR 설명, 코드 주석, 앱과 웹의 화면 문구, 발표 자료, 사용자에게 보내는 대화 응답이 전부 여기 든다.

적용하지 않는 경우는 둘뿐이다. 남의 글을 원문 그대로 인용할 때와, 기계 생성물을 기계 생성물인 채로 다룰 때다.

## 적용 환경

- **Claude 앱(chat)**: 이 스킬의 주 무대다. 훅이 없으므로 이 문서와 `standards/` 가 강제 장치의 전부다. 스킬이 로드되면 아래 절차를 그대로 따르고, 한국어 산출물마다 해당 장르 팩을 실제로 연다.
- **Claude Code**: `install/` 의 훅과 전역 규칙이 이 문서를 보강한다. 훅은 금지 표현을 결정론으로 막고, 전역 규칙은 스킬을 부르지 않은 세션에도 상주한다. 남의 글이나 기계 생성물을 다룰 때는 `NATIVE_OUTPUT_GATE=off` 로 훅을 끈다.

## 절차

### 1. 무엇을 쓸지 먼저 정한다

`standards/judgment.md` 의 사전 질문 사슬을 답한다.

왜 쓰는가 → 어디에 실리고 누가 읽는가 → 메시지 → 들어가야 할 내용 → 형식 → 문장.

- 이 사슬의 답에서 **장르와 화계가 나온다.** 장르를 먼저 정해 놓고 사슬을 거꾸로 채우지 않는다.
- 고칠 글이 오면 손대기 전에 목적에 비춰 내용부터 판정한다. 빠진 것과 군더더기를 먼저 보고, 내용이 목적에 안 맞으면 재작성 전에 그 사실을 보고한다.
- 사용자가 답을 주지 않은 고리가 있으면 추측해 채우지 않는다. 답 없이 진행할 수 있는 부분은 먼저 쓰고 막힌 고리만 묻는다.

### 2. 층을 겹쳐 적용한다

아래 순서로 겹쳐 적용하고, 충돌하면 더 구체적인 층이 이긴다.

| 층 | 파일 | 무엇을 정하나 |
|---|---|---|
| 판단 | `standards/judgment.md` | 무엇을 쓸지. 근거 규율. 항상 먼저 |
| 문장 | `standards/korean.md` | 번역투. 무생물 주어, 이중 피동, "~을 갖는다" |
| 글 | `standards/discourse.md` | 시작과 전개와 호흡과 끝. 제목과 소제목 |
| 표현 | `standards/style.md` | 화계 통일, 표기, AI 상투 표현 |
| 장르 | `standards/genres.md` → `standards/genres/<장르>.md` | 장르 판정과 팩. 리듬 수치와 화계 분포와 허용·금지 표현 |

산문이 아니면 위에 전용 층을 하나 더 겹친다.

| 쓰는 것 | 파일 |
|---|---|
| 하네스 문서, 코드 주석, 결정 기록, 명세와 규정 | `standards/dev-prose.md` |
| 앱과 웹의 화면 문구. 버튼, 라벨, 빈 상태, 확인 대화상자 | `standards/ui-copy.md` |
| 발표 자료. 슬라이드 텍스트와 대본 | `standards/slides.md` |

한 벌로 통과한다. 같은 검사를 여러 번 겹치지 않는다.

### 3. 자가 검사

쓴 뒤에 셋을 확인한다.

- **화계가 한 벌인가.** 캐주얼 단문 말고는 예외가 없다.
- **리듬이 장르 팩 근처인가.** 그 장르의 `genres/<장르>.md` 수치와 대조한다. 중앙값이 ±5자 안이면 맞은 것이다. 두 번 조정해도 안 맞으면 더 손대지 말고 수치를 그대로 보고한다.
- **걸린 표현이 이 장르에서 정말 이상한가.** `genres.md` 의 장르별 표로 판정한다. 이 장르의 잘 쓴 글에 실제로 나오는 표현이면 지우지 않는다.

### 4. 반환

공개용이거나 고위험 글이면 이 글을 처음 보는 별도 에이전트를 불러 검수한다. 브리프는 `guides/review.md` 를 그대로 넘긴다.

## 계약

협상 불가 항목이다.

- **기준은 필터가 아니다.** 최종 판정은 그 문장이 자연스러운 한국어인가이지 금지 목록에 걸렸는가가 아니다. 규칙을 기계로 적용해 새 어색함을 만들지 않는다. "위계를 가진다" 처럼 이미 자연스러운 문장을 금지 규칙 때문에 망가뜨리지 않는다.
- **일괄 치환하지 않는다.** 교정은 원문을 읽고 문장 단위로 다시 쓴다. 스크립트로 문장이나 기호를 바꾸지 않는다.
- **수치를 지어내지 않는다.** `genres.md` 의 리듬 수치는 실제 글을 측정한 기본값이다. 새 수치를 임의로 적어 넣지 않고, 목록에 없는 장르는 가장 가까운 장르로 대용한 뒤 대용 사실을 밝힌다.
- **금지 항목은 실제 반려가 관측될 때만 늘린다.** 있을 법한 표현을 미리 넣으면 오탐이 쌓이고, 오탐이 쌓이면 아무도 검사를 켜 두지 않는다.
- **사용자가 예시 글을 주면 그것이 이긴다.** 이미 승인된 원문이 있으면 재작성보다 그대로 쓰는 쪽이 안전하다.
- **에이전트의 대화 말투도 대상이다.** 다만 장르 층은 적용하지 않는다. 표현과 표기의 기본값과 금지 목록만 적용한다.

## 보존형 윤문 (특수 모드)

남의 글, 사실과 주장을 고정해야 하는 확정 원고, 사용자가 보존을 명시한 글에만 적용한다. 다른 모드에 확대 적용하지 않는다.

- 의미 불변이 절대다. 사실과 주장과 수치와 인용과 순서를 고정한다.
- 변경률 5~30% 밴드를 지킨다. 5% 미만은 덜 고친 것이고, 30% 초과는 과윤문이므로 롤백을 검토한다.
- 뭉개는 명사를 바꿀 때는 대체어의 근거를 원문의 단정 부분에서만 가져온다. 유보나 추측 문장에서 끌어오지 않는다. 막히면 명사를 다른 명사로 바꾸지 말고 술어로 푼다.
- 마치면 `guides/review.md` 의 절차로 독립 검수한다.

작성과 유연한 고치기에는 이 밴드가 없다. 그쪽 게이트는 목적 적합성이고 변경 규모에 상한이 없다.

## 설치와 배선 (Claude Code 전용)

Claude 앱에서는 스킬 업로드로 끝이고 이 절은 해당하지 않는다. Claude Code 설치 절차는 `guides/setup.md` 가 갖는다. `/native-output setup` 으로 실행한다.

배선은 두 겹이다. 스킬 호출이 규칙 준수를 보장하지 않으므로 상주 규칙과 결정론 훅을 함께 건다.

- `install/rules/native-output.md` 가 `~/.claude/rules/` 에 설치되어 세션마다 자동 로드된다. 스킬을 부르지 않아도 이 규칙이 상주한다.
- `install/hooks/native-output-gate.js` 가 한글이 든 Write 와 Edit 직전에 `standards/banned-words.json` 을 대조한다.
- `install/hooks/native-output-stop-gate.js` 가 턴 끝에 마지막 대화 응답을 같은 목록과 대조한다.
- 강도는 `block`(거부, 기본 권장), `warn`(알림만), `off`(훅 없음) 셋이다. 기준이 실제로 강제되는 것은 `block` 뿐이다. `node install/scripts/setup-hooks.js --level=<강도>` 로 바꾼다.

## 팀 기준으로 고치기

`standards/` 는 고쳐 쓰라고 있는 폴더다. 사용자가 기준을 바꿔 달라고 하면 `guides/customize.md` 를 따른다.
